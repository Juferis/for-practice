type Movie {
  id: Int!
  title: String!
  rating: Float!
  summary: String
  language: String!
  medium_cover_image: String!
}

type Query {
  movies(limit: Int, rating: Float): [Movie]!
}
// Mutation은 Database 상태가 변할 때 사용한다.